export declare type FolderStructureContent = string | {} | FolderStructure;
export declare type FolderStructure = {
    [name: string]: FolderStructureContent;
};
export declare type EntryStructure = {
    entryName?: string;
    content: FolderStructureContent;
};
export declare function createFolder(content?: FolderStructure): Promise<string>;
export declare function createFile(content?: string | {}): Promise<string>;
export default function createFolderStructure(structure: EntryStructure): Promise<{
    entryPath: string;
    cleanup: () => Promise<void>;
}>;
